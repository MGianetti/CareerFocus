import React, { ReactNode } from "react";

interface Props {
  children?: ReactNode;
  category: string;
}

const Collapser: React.FC<Props> = () => {
  return <div></div>;
};

export default Collapser;

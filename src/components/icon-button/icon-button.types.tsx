export type IconButtonProps = {
  backgroundColor?: string;
  color?: string;
  icon: "add" | "chevron" | "close" | "hamburguer" | "remove" | "search";
  onClick?: () => void;
  position?: string;
  size?: number;
  margin?: string;
};
